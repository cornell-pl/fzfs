(jbuild_version 1)

(alias
 ((name runtest)
  (action
   (progn
    (run ${exe:baby.exe})
  ))
 )
) ; Need to run server first


(executables
 ((names (baby))
  (public_names (baby))
  (libraries (core async DTZ))
  (preprocess (pps (ppx_jane)))
 ))
